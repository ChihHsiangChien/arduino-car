/*
 * 函式庫製作教學 (Library Creation Tutorial)
 * 
 * 目的：
 * 當一個程式碼變得很長、很複雜時（例如手寫紅外線解碼），
 * 我們會希望把它「打包」起來，讓主程式 (main sketch) 看起來乾淨清爽。
 * 
 * 在 C++ 中，這通常分為兩個檔案：
 * 
 * 1. 標頭檔 (.h)：像是「菜單」
 *    - 列出這支程式提供了哪些功能？ (函數名稱、變數名稱)
 *    - 不包含實際的運作邏輯。
 *    - 放在 `SimpleIR.h`
 * 
 * 2. 實作檔 (.cpp)：像是「廚房」
 *    - 實際寫出每個功能是怎麼運作的 (邏輯、數學運算)。
 *    - 放在 `SimpleIR.cpp`
 * 
 * 使用方式：
 * 在主程式 (`car.ino`) 中，只要寫 `#include "SimpleIR.h"`，
 * 電腦就會自動去連結這兩個檔案，我們就可以像使用積木一樣直接呼叫功能。
 * 
 * 你的專案結構現在應該長這樣：
 * /arduino-car/
 *   ├── car.ino        (主程式：乾淨簡單)
 *   ├── SimpleIR.h     (菜單：告訴編譯器有 ir.check() 這功能)
 *   └── SimpleIR.cpp   (廚房：裡面藏著複雜的脈衝時間計算)
 */

void setup() {
  // 這個檔案只是純教學說明，不需要上傳。
  // 請直接閱讀上方的註解。
}

void loop() {
}
